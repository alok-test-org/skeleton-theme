name: CI
on: [push]
jobs:
  theme-check:
    name: Theme Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check it
        uses: alok-test-org/theme-check-action@test-me
        with:
          token: ${{ github.token }}
          theme_root: ./theme
